# 复制成 .env.local（已 gitignore）后按需改。
# 全部都有可用的缺省值，什么都不用配就能连本地后端。

# ══ 本地开发（npm run dev）════════════════════════════════════════
#
# 默认打开 mock 假数据。`npm run dev:live` 连真实后端。

# manager 后端地址（dev:live 模式的代理目标）。默认 http://localhost:3000。
# VITE_MANAGER_ORIGIN=http://localhost:3000

# 开发端口，默认 5180。
# VITE_DEV_PORT=5180

# 其他也想放行的本地域名，逗号分隔（Vite 6 会按 Host 头拦截未列出的域名）。
# VITE_DEV_EXTRA_HOSTS=

# ══ 构建（npm run build）═══════════════════════════════════════════
#
# Docker 部署时前后端同源，不需要配任何变量。
#
# 如果要挂在反向代理子路径下：
# VITE_BASE_PATH=/sandbox-manager
# VITE_API_PREFIX=/api/sandbox-manager
#
# 如果前后端跨域部署：
# VITE_API_ORIGIN=http://manager-api.example.com
